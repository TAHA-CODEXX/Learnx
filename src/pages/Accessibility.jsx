import React from 'react';
import StaticPage from './StaticPage';

const Accessibility = () => {
    return (
        <StaticPage
            title="Accessibilité"
            description="S'assurer que Learnx est accessible à tous est au cœur de notre mission."
        />
    );
};

export default Accessibility;
